import features


def test_determine_lcc():
    graph =
    cell_type_assignments =
    slide_submitter_id =

    features.determine_lcc(graph = graph, cell_type_assignments= cell_type_assignments, slide_submitter_id= slide_submitter_id)
